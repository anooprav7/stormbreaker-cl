import React from 'react';
import { withTheme } from '../themeDecorator';
import { Box } from '../../src/layout/Box';
import { Button } from '../../src/components/Button';

export const BoxDoc = withTheme(Box);
export const ButtonDoc = withTheme(Button);

